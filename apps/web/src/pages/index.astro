---
import BaseLayout from '../layouts/BaseLayout.astro';
import CheckIcon from '../components/CheckIcon';

const title = "aipm";
const description = "Claude Code is for coding. This is for PM-ing.";

const features = [
  {
    title: "Natural language task management",
    desc: "Just type what you need. The AI understands intent and executes through tool calls."
  },
  {
    title: "Email inbox -> task list",
    desc: "Surfaces actionable emails via AI. Marketing and noise filtered out automatically."
  },
  {
    title: "File-per-task architecture",
    desc: "Each task is a markdown file with YAML front matter. Grep your way through tasks."
  },
  {
    title: "Full keyboard control",
    desc: "Vim-style bindings. Arrow keys. No mouse needed. Navigate instantly."
  },
  {
    title: "Multiple views",
    desc: "Buckets (columns), Timeline (chronological), Kanban (progress stages)."
  },
  {
    title: "Context-aware AI",
    desc: "Paste GitHub URLs, AI fetches context automatically. No manual copying."
  },
  {
    title: "Sub-tasks & dependencies",
    desc: "Hierarchical tasks with automatic parent progress sync."
  },
  {
    title: "Undo/history",
    desc: "Snapshot before every operation. Rollback anytime."
  }
];
---

<BaseLayout title={title} description={description}>
  <section class="hero">
    <div class="hero-grid">
      <div class="hero-copy">
        <p class="section-kicker"><span class="kicker-dot"></span>vision</p>
        <h1 class="hero-title">Run every project with execution-grade AI orchestration.</h1>
        <p class="hero-tagline">From inbox to roadmap to shipped work, every task stays local and every action can be executed through real tools.</p>
        <p class="hero-desc">Built for operators who care about speed, clarity, and control inside the terminal.</p>
        <pre
          class="install-cmd"
          onclick="navigator.clipboard.writeText('brew tap ComputelessComputer/aipm && brew install aipm'); const prompt = this.querySelector('.terminal-prompt'); if (!prompt) return; prompt.classList.add('copied'); this.classList.add('copied'); clearTimeout(this.copyTimer); this.copyTimer = setTimeout(() => { prompt.classList.remove('copied'); this.classList.remove('copied'); }, 1200);"
          title="Click to copy"
        ><code><span class="terminal-prompt" aria-hidden="true"><span class="prompt-dollar">$</span><span class="prompt-check"><CheckIcon size={16} strokeWidth={2.5} /></span></span><span>brew tap ComputelessComputer/aipm && brew install aipm</span></code></pre>
      </div>

      <div class="hero-visual" aria-hidden="true">
        <span class="ribbon ribbon-a"></span>
        <span class="ribbon ribbon-b"></span>
        <span class="ribbon ribbon-c"></span>
        <span class="ribbon ribbon-d"></span>
      </div>
    </div>
  </section>

  <section class="value-props">
    <p class="section-kicker"><span class="kicker-dot"></span>foundation</p>
    <div class="value-grid">
      <div class="prop">
        <h3>local-first</h3>
        <p>Your tasks live as plain files on your filesystem. No servers, no sync, no lock-in. Grep, sed, or script your way through everything.</p>
      </div>
      <div class="prop">
        <h3>cli-first</h3>
        <p>Headless commands with JSON output. Built for scripting and AI coding agents. Works as well in a pipeline as it does interactively.</p>
      </div>
      <div class="prop">
        <h3>super fast</h3>
        <p>Native Rust TUI. Instant startup, zero latency navigation, no Electron, no browser. Just your terminal.</p>
      </div>
    </div>
  </section>

  <section class="demo">
    <p class="section-kicker"><span class="kicker-dot"></span>demo</p>
    <h2>Watch aipm in flow</h2>
    <div class="video-container">
      <iframe
        src="https://www.youtube.com/embed/PGFg01h9LHU"
        title="aipm demo"
        frameborder="0"
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
        allowfullscreen
      ></iframe>
    </div>
  </section>

  <section class="features">
    <p class="section-kicker"><span class="kicker-dot"></span>features</p>
    <h2>Agent-native task operations</h2>
    <div class="features-grid">
      {features.map((f) => (
        <div class="feature">
          <h3>{f.title}</h3>
          <p>{f.desc}</p>
        </div>
      ))}
    </div>
  </section>
</BaseLayout>

<style>
  .section-kicker {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    margin-bottom: 1.1rem;
    font-family: var(--font-mono);
    font-size: 0.73rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: #6f7896;
  }

  .kicker-dot {
    width: 0.45rem;
    height: 0.45rem;
    border-radius: 999px;
    background: linear-gradient(135deg, #6d63ff, #ff8a4c);
  }

  .hero {
    position: relative;
    padding: clamp(1.8rem, 3.8vw, 3rem);
    padding-bottom: clamp(2.8rem, 5vw, 4.8rem);
    border: 1px solid #dde2f4;
    border-radius: 16px;
    background: linear-gradient(180deg, #ffffff 0%, #f8faff 100%);
    color: #121a3c;
    text-align: left;
    overflow: hidden;
  }

  .hero .section-kicker {
    color: #646e95;
  }

  .hero .kicker-dot {
    background: linear-gradient(135deg, #6d63ff, #ff8a4c);
  }

  .hero-grid {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1fr;
    gap: clamp(1.8rem, 3.5vw, 3.4rem);
    justify-items: start;
  }

  .hero-copy {
    max-width: 760px;
    justify-self: start;
  }

  .hero-title {
    margin-bottom: 1rem;
    line-height: 0.95;
    font-size: clamp(2.35rem, 6.2vw, 5.25rem);
    max-width: 14ch;
  }

  .hero-tagline {
    margin-bottom: 1rem;
    max-width: 53ch;
    font-size: clamp(1.1rem, 1.9vw, 1.8rem);
    line-height: 1.22;
    color: #5c6590;
  }

  .hero-desc {
    max-width: 52ch;
    margin-bottom: 1.45rem;
    font-size: clamp(0.95rem, 1.2vw, 1.15rem);
    line-height: 1.5;
    color: #4e5882;
  }

  .install-cmd {
    display: inline-block;
    width: fit-content;
    max-width: 100%;
    text-align: left;
    cursor: pointer;
    border-radius: 10px;
    border: 1px solid #ccd3f0;
    background: #f7f9ff;
    box-shadow: 0 8px 18px rgba(18, 27, 70, 0.08);
    padding: 0.9rem 1rem;
    transition: border-color var(--transition-fast), background-color var(--transition-fast);
  }

  .install-cmd:hover {
    border-color: #adb8ea;
    background: #ffffff;
  }

  .install-cmd code {
    display: flex;
    align-items: center;
    color: #1b2450;
    font-size: clamp(0.86rem, 1.45vw, 1rem);
    line-height: 1.35;
    white-space: nowrap;
    overflow-x: auto;
    background: none;
    border: 0;
    padding: 0;
  }

  .terminal-prompt {
    display: inline-flex;
    align-items: center;
    color: #5a54e8;
    margin-right: 0.6ch;
    user-select: none;
    -webkit-user-select: none;
  }

  .prompt-check {
    display: none;
    line-height: 0;
  }

  .terminal-prompt.copied .prompt-dollar {
    display: none;
  }

  .terminal-prompt.copied .prompt-check {
    display: inline-flex;
  }

  .install-cmd.copied .terminal-prompt {
    color: #13a66d;
  }

  .hero-visual {
    position: relative;
    border: 1px solid #d8def3;
    background: linear-gradient(180deg, #f7f9ff 0%, #f2f6ff 100%);
    border-radius: 12px;
    min-height: 280px;
    overflow: hidden;
    justify-self: stretch;
    width: 100%;
  }

  .ribbon {
    position: absolute;
    border-radius: 999px;
    transform-origin: center;
    filter: saturate(110%);
  }

  .ribbon-a {
    width: 520px;
    height: 140px;
    right: -170px;
    top: 20px;
    transform: rotate(-36deg);
    background: linear-gradient(90deg, rgba(96, 82, 255, 0) 0%, rgba(96, 82, 255, 0.78) 44%, rgba(255, 128, 72, 0.9) 100%);
  }

  .ribbon-b {
    width: 540px;
    height: 160px;
    right: -170px;
    top: 110px;
    transform: rotate(-36deg);
    background: linear-gradient(90deg, rgba(255, 71, 145, 0) 4%, rgba(255, 71, 145, 0.76) 45%, rgba(255, 183, 42, 0.9) 100%);
  }

  .ribbon-c {
    width: 560px;
    height: 120px;
    right: -190px;
    top: 205px;
    transform: rotate(-36deg);
    background: linear-gradient(90deg, rgba(110, 93, 255, 0) 0%, rgba(110, 93, 255, 0.74) 46%, rgba(255, 97, 173, 0.78) 100%);
  }

  .ribbon-d {
    width: 450px;
    height: 80px;
    right: -150px;
    top: 14px;
    transform: rotate(-36deg);
    opacity: 0.55;
    background: linear-gradient(90deg, rgba(255, 200, 96, 0) 0%, rgba(255, 200, 96, 0.8) 52%, rgba(255, 141, 86, 0.85) 100%);
  }

  .value-props,
  .demo,
  .features {
    padding-top: clamp(2.2rem, 4.5vw, 4.2rem);
  }

  .value-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
  }

  .prop {
    border: 1px solid var(--color-border);
    background: #ffffff;
    border-radius: 10px;
    padding: 1.25rem;
  }

  .prop h3 {
    font-size: 1.18rem;
    margin-bottom: 0.55rem;
    color: var(--color-text);
  }

  .prop p {
    font-family: var(--font-mono);
    font-size: 0.95rem;
    color: var(--color-secondary);
    line-height: 1.6;
  }

  .demo h2,
  .features h2 {
    margin-bottom: 1.2rem;
    max-width: 16ch;
  }

  .video-container {
    position: relative;
    padding-bottom: 56.25%;
    height: 0;
    overflow: hidden;
    border-radius: 12px;
    border: 1px solid var(--color-border);
    background: #f8faff;
  }

  .video-container iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
  }

  .features-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
  }

  .feature {
    border: 1px solid var(--color-border);
    background: #ffffff;
    border-radius: 10px;
    padding: 1.15rem;
  }

  .feature h3 {
    font-size: 1.02rem;
    margin-bottom: 0.4rem;
  }

  .feature p {
    font-family: var(--font-mono);
    font-size: 0.88rem;
    color: var(--color-secondary);
    line-height: 1.5;
  }

  @media (min-width: 900px) {
    .hero-grid {
      grid-template-columns: minmax(0, 1.2fr) minmax(0, 0.8fr);
      align-items: stretch;
    }

    .value-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .features-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 640px) {
    .hero-tagline {
      font-size: 1rem;
    }

    .install-cmd {
      display: block;
      width: 100%;
      padding: 0.8rem 0.88rem;
    }

    .install-cmd code {
      justify-content: flex-start;
    }

    .hero-visual {
      min-height: 220px;
    }

    .ribbon-a,
    .ribbon-b,
    .ribbon-c,
    .ribbon-d {
      right: -220px;
      transform: rotate(-39deg);
    }
  }
</style>
