# Google OAuth credentials for local AIPM testing
# Create a Desktop app OAuth client in Google Cloud, then paste values below.
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Optional: override where AIPM stores local data/tokens
# AIPM_DATA_DIR=/absolute/path/to/aipm-data
